{
  "id": "rocm_accelerator",
  "name": "AMD GPU Hardware Acceleration",
  "author": "Schaka",
  "description": "Requires the AudioMuse-AI ROCm worker image; inert on other images. Runs musicnn and the CLAP audio encoder on the AMD GPU via ONNX Runtime's MIGraphX provider, swaps lyrics ASR to a GPU-backed engine (faster-whisper/CTranslate2, whisper.cpp, or parakeet.cpp) instead of the built-in ONNX Whisper, and replaces the cuML GPU clustering path with a native PyTorch/ROCm backend (KMeans + PCA on the GPU, DBSCAN/GMM/spectral on CPU). Per-arch profiles handle generation differences (fp16, blocked backends, etc). Docs: https://github.com/Schaka/audiomuse-rocm-plugin/blob/main/plugin/rocm_accelerator/README.md",
  "version": "0.0.0-dev",
  "min_core_version": "3.1.0",
  "targets": ["worker"],
  "requirements": [],
  "changelog": "Proof of concept: MIGraphX provider for musicnn + faster-whisper ASR backend + native ROCm clustering backend."
}
